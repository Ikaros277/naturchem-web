/**
 * Copy for the /typicke-zakazky page.
 *
 * Page purpose: visitors should see what measurements, studies and documentation
 * NATURCHEM provides for operations; then find their situation and project workflow.
 */

/** Eyebrow — hero context (what the page shows). */
export const typickeZakazkyEyebrow = "Measurements, studies and documentation from practice";

/** Combined hero lead — inspiration + what visitors find on the page (single layer). */
export const typickeZakazkyHeroLead =
  "What we measure and prepare for operations — from emissions to studies, EIA and ISPOP.";

/** Scenarios section heading — "what you need" without repeating in cards. */
export const typickeZakazkyScenariosHeading = "What we address";

/** Examples section heading (modal on the same page). */
export const typickeZakazkyExamplesHeading = "Project examples from practice";

export const typickeZakazkyExamplesIntro =
  "Short stories from anonymised projects — they open here without leaving the page.";
